import produce from 'immer';
import { createReducer, ActionType, getType } from 'typesafe-actions';

import * as actions from './actions';

export type {{ properCase name }}State = Readonly<{
  domain: string;
}>;

export const initialState: {{ properCase name }}State = {
  domain: '',
};

function defaultActionHandler(
  state: {{ properCase name }}State,
  action: ActionType<typeof actions.defaultAction>,
) {
  return produce(state, draft => {
    draft.domain = action.payload.domain;
  });
}

export default createReducer(initialState, {
  [getType(actions.defaultAction)]: defaultActionHandler,
});
