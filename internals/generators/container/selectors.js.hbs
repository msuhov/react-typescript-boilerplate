import { createSelector } from 'reselect';
import { prop } from 'ramda';

import { RootState } from 'store';

import { initialState } from './reducer';

export const select{{ properCase name }} = (state: RootState) => state.{{ camelCase name }} || initialState;

export const select{{ properCase name }}Domain = createSelector(
  select{{ properCase name }},
  prop('domain'),
);
