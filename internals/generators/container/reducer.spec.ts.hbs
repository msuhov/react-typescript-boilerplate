// import produce from 'immer';
import {{ camelCase name }}Reducer, { {{ properCase name }}State, initialState } from '../reducer';
// import { someAction } from '../actions';

describe('{{ camelCase name }}Reducer', () => {
  let state: {{ properCase name }}State;
  beforeEach(() => {
    state = initialState
  });

  it('returns the initial state', () => {
    const expectedResult = state;
    expect({{ camelCase name }}Reducer(undefined, {})).toEqual(expectedResult);
  });

  it('Expect to have additional unit tests specified', () => {
    expect(true).toEqual(false);
  });

  /**
   * Example state change comparison
   *
   * it('should handle the someAction action correctly', () => {
   *   const expectedResult = produce(state, draft => {
   *     draft.loading = true;
   *     draft.error = false;
   *     draft.userData.nested = false;
   *   });
   *
   *   expect(appReducer(state, someAction())).toEqual(expectedResult);
   * });
   */
});
